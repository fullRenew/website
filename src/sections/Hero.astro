---
import WhatsAppButton from "@/components/WhatsAppButton.astro";

const whatsappMessage = {
    url: 'https://wa.me/',
    phone: '604846040',
    text: 'Hola, quiero saber más sobre sus servicios'
}

const whatsappHref = `${whatsappMessage.url}${whatsappMessage.phone}?text=${whatsappMessage.text}`
---
<section class="flex flex-col gap-6 md:gap-12">
    <h1 class="text-2xl md:text-6xl font-medium text-white leading-snug">
        Impulsa tu negocio con iPhones reacondicionados usados de calidad: stock amplio, entregas rápidas y garantía confiable.
    </h1>
    <p class="text-neutral-300 md:text-xl md:w-1/2">
        Descubre la mejor selección de iPhones reacondicionados en España. Conoce nuestro stock, precios y garantías.
    </p>
    <span class="w-full flex flex-row justify-end md:justify-start">
        <WhatsAppButton whatsappHref={whatsappHref}>
            Saber Mas
        </WhatsAppButton>
    </span>
</section>